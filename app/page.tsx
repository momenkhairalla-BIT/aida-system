// The home page renders the Aida UI Direction Preview directly so the base
// URL works on static hosting (GitHub Pages). The dedicated route
// /aida-ui-direction remains available as well.
export { default, metadata } from "./aida-ui-direction/page";
